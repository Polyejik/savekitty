const API='https://spasipushka-api.kutuzovap.workers.dev/dashboard?campaign=save-pushok-pilot',q=id=>document.getElementById(id),nf=n=>new Intl.NumberFormat('ru-RU').format(Math.round(Number(n)||0)),money=n=>nf(n)+' ₽',tm=s=>{s=Math.round(Number(s)||0);return Math.floor(s/60)+':'+String(s%60).padStart(2,'0')};function blank(){['confirmed','donation','players','today','avg','repeat','raw','valid','dup','fast','rate','totalBudget','remain','days'].forEach(id=>q(id).textContent='—');q('budget').textContent='—';q('runs').innerHTML=''}function render(d){const s=d.summary;q('confirmed').textContent=nf(s.confirmed_rescues);q('donation').textContent=money(s.donation_rub);q('budget').textContent='из '+money(s.budget_rub);q('players').textContent=nf(s.unique_players);q('today').textContent=nf(s.today_runs);q('avg').textContent=tm(s.avg_seconds);q('repeat').textContent=nf(s.repeat_rate)+'%';q('raw').textContent=nf(s.raw_runs);q('valid').textContent=nf(s.confirmed_rescues);q('dup').textContent=nf(s.duplicates);q('fast').textContent=nf(s.too_fast);q('rate').textContent=money(s.rate_rub);q('totalBudget').textContent=money(s.budget_rub);q('remain').textContent=money(Math.max(0,s.budget_rub-s.donation_rub));q('days').textContent=nf(s.days_left)+' дн.';q('runs').replaceChildren();for(const r of d.runs||[]){const tr=document.createElement('tr');for(const value of [new Date(r.completed_at).toLocaleString('ru-RU'),r.participant||'Участник',tm(r.duration_seconds),r.game_version||'—',r.status||'confirmed']){const td=document.createElement('td');td.textContent=value;tr.appendChild(td)}q('runs').appendChild(tr)}q('state').textContent='LIVE';q('state').style.color='#72c68b';q('err').style.display='none';q('updated').textContent='Обновлено '+new Date().toLocaleTimeString('ru-RU')}let loading=false;async function load(){if(loading)return;loading=true;const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),12000);try{const r=await fetch(API,{cache:'no-store',signal:controller.signal});if(!r.ok)throw new Error('HTTP '+r.status);const d=await r.json();if(!d.ok)throw new Error(d.error||'API');render(d)}catch(e){blank();q('state').textContent='НЕТ СВЯЗИ';q('state').style.color='#e88175';q('err').style.display='block';q('updated').textContent='Данные не загружены'}finally{clearTimeout(timer);loading=false}}load();setInterval(load,30000);
