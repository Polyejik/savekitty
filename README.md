# Save Kitty / Спаси Пушка

Educational long-division mini-game, RU/EN.

`progress.csv` defines the lightweight progress schema. GitHub Pages is static, so each browser stores progress as CSV text in `localStorage` under `savekitty_progress_csv_v1`. No GitHub write token is exposed to players.
